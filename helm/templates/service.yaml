apiVersion: v1
kind: Service
metadata:
  name: {{ include "painel.fullname" . }}
  labels:
    {{- include "painel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
      name: http
  selector:
    {{- include "painel.selectorLabels" . | nindent 4 }}
