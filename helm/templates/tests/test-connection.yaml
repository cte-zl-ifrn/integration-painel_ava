apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "painel.fullname" . }}-test-connection"
  labels:
    {{- include "painel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "painel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
